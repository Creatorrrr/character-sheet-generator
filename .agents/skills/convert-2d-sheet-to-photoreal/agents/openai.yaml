interface:
  display_name: "2D Sheet to Photoreal"
  short_description: "Create sheet-annotation-free and text-restored photoreal sheets"
  default_prompt: "Use $convert-2d-sheet-to-photoreal to convert this 2D character sheet into a layout-locked sheet-annotation-free photoreal sheet while preserving in-image costume/prop typography, then restore the original sheet annotation text onto it."
