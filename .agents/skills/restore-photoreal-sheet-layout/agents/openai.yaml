interface:
  display_name: "Restore Photoreal Sheet Text"
  short_description: "Add original text to locked photoreal sheet"
  default_prompt: "Use $restore-photoreal-sheet-layout to restore the original sheet text onto this text-free photoreal sheet without changing layout or character content."
